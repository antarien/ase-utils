# Stub CMakeLists.txt for ase-utils
# TODO: Implement actual build logic

cmake_minimum_required(VERSION 3.20)
project(ase-utils VERSION 0.1.0 LANGUAGES CXX)

message(STATUS "Stub: ase-utils (not implemented yet)")

# Placeholder target
add_custom_target(ase-utils_stub ALL
    COMMAND ${CMAKE_COMMAND} -E echo "Module ase-utils is a stub"
)
